{{!< default}}

{{#post}}
  <header class="intro-header" {{#if image}}style="background:no-repeat center center;background-image:url({{image}})"{{/if}}>
      <div class="container">
          <div class="row">
              <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                  <div class="post-heading">
                      <h1>{{{title}}}</h1>
                      {{#author}}
                          <span class="meta">Posted by <a href="{{url}}">{{name}}</a> on {{date format="MMMM DD, YYYY"}}</span>
                      {{/author}}
                  </div>
              </div>
          </div>
      </div>
  </header>

  <article id="{{id}}" class="{{post_class}}">
      <div class="container">
          <div class="row">
              <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                   {{content}}
              </div>
          </div>
      </div>
  </article>
{{/post}}

<hr>