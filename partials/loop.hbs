{{#foreach posts}}
  <article id="{{id}}" class="post-preview">
    <a href="{{url}}">
      <h2 class="post-title">
        {{{title}}}
      </h2>
      <section class="post-subtitle">
        {{excerpt}}
      </section>
    </a>

    <p class="post-meta">Published by <a href="{{url}}">{{author}}</a> on <time datetime="{{published_at}}">{{date format="MMMM DD, YYYY"}}</time></p>
  </article>
  <hr>
{{/foreach}}
